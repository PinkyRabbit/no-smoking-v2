export enum DialogKey {
  beginning = "new_user_beginning"
}

/**
 * Button names
 */
export enum BTN {
  Beginning = "new_user_beginning"
}

export enum Callback {
  beginning = "new_user_beginning"
}